% Split FIDs into two arrays of double precision floats with 70 rows
FIDs_coil1 = FIDs(1:70, :);
FIDs_coil2 = FIDs(71:140, :);


%{
plot(abs(FIDs(:,1:30)),'LineWidth',1)
legend("proj1","proj2","...")
xlabel("Readout")
ylabel("Intensity [a.u.]")
xlabel("Projection")
%